<script type="text/javascript">
    $(document).ready(function(){
        $("form").submit(function ()
        {
            return false;
        });
        $(".active").click(function(){
            var search = $(".form-control").val();
            $.post("admin/search",
                {
                    search: search
                }, function(data)
                {
                    $('#result').html(data);
                });
        });
    })
</script>
<div class="container">
<div class="col-lg-12">
    <ul class="pagination">
        <li class="active"><a href="<?php echo "http://".$_SERVER['SERVER_NAME'].'/admin';?>">Пользователи</a></li>
        <li><a href="<?php echo "http://".$_SERVER['SERVER_NAME'].'/categories';?>">Категории</a></li>
        <li><a href="<?php echo "http://".$_SERVER['SERVER_NAME'].'/categories/show';?>">Объявления</a></li>
    </ul>
    <table class="table table-striped" id="result"></table>
    <form class="form-inline" role="form">
            <div class="form-group">
                <label for="text">Поиск</label>
                <input type="text" name="desc" class="form-control" placeholder="Логин пользователя, заголовок публикации или категорию" size="60" required>
            </div>
            <button type="submit" class="active">Search</button>
    </form>
    <table class="table table-striped">
        <thead>
        <tr>
            <th>Логин</th>
            <th>Телефон</th>
            <th>Статус</th>
            <th>Опубликовано</th>
            <th>Дата регистрации</th>
            <th>Действия</th>
        </tr>
        </thead>
        <tbody>
        <?php foreach ($row as $tt): ?>
            <tr>
                <td><?php echo $tt["login"]; ?></td>
                <td><?php echo $tt["phone"]; ?></td>
                <td><?php echo $tt["status"]; ?></td>
                <td><?php echo $tt["caunt"]; ?></td>
                <td><?php echo $tt["date_create"]; ?></td>
                <td>
                        <a href="#">SHOW |</a>
                    <?php if($tt['status']=='banned'):?>
                        <a href="/admin/unban/<?php echo $tt["id"]; ?>">UNBAN</a>
                    <?php else:?>
                        <a href="/admin/ban/<?php echo $tt["id"]; ?>">BAN</a>
                    <?php endif; ?>
                </td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>
</div>